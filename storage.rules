
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only allow authenticated users to read/write to the "images" folder.
    match /images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
